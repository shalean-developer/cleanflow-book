/**
 * Supabase client - single source of truth for auth and database
 * Re-exports the configured client from integrations for consistency
 */
export { supabase } from '@/integrations/supabase/client';

